import { DatePostedPipe } from './date-posted.pipe';

describe('DatePostedPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePostedPipe();
    expect(pipe).toBeTruthy();
  });
});
